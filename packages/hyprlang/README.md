# hyprlang

Rebase of the official Fedora source package to upstream tag `v0.6.8`.

The runtime package carries SOVERSION 2. The development package contains the
single public header and pkg-config metadata. The downstream patch only
removes hard-coded `-O3`.
